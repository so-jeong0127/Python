<수학함수>
- round() : 반올림 함수

<3항 연산자>
a = '조건이 참인 경우' if조건문 '조건이 거짓인 경우'

<if문 참인 경우>
- 숫자 : 0
- 문자열 : ' '(공백이 생길 경우)
- list: [], dict: {}, tuple: ()

<날짜함수>
import datatime
datatime.datatime.now()

# Collections: list, tuple, dict, set
#iterator: range -> (i=0; i<100; i++)와 같은 방법으로 사용 (start, end, step)
- 반대로 출력해 주는 함수:  reversed()

#generator: 데이터 생성, 메모리를 차지하지 않음
#enumerate: 매핑을 해줌 (0: earth, 1: air, 2: fire, 3: water)

- 내장리스트: 동적으로 데이터 생성
 [i for i in range(10)]

# 정렬하고 싶은 경우 (이름으로 정렬)
- def getKey(a1):
      return a1[1]
  print.sort(key=getKey)
  print(~)
- final.sort(key=lambda row: row[1], reverse = True) # 내림차순으로 정리

# 기본데이터 타입: str, int, float, list, dict, set, tuple
# 파이썬은 모든 것이 객체(변수+함수) : 사용자 정의 데이터 타입